{% form_theme form 'bootstrap_3_layout.html.twig' %}
{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
<div class="col-lg-4 col-md-4 col-md-offset-4 ">
    <h1>Création d'un contact </h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit"  class="btn btn-info center-block"  value="Creer" />
    {{ form_end(form) }}

</div>
</div>
    <ul></ul>
    <a href="{{ path('contact_index') }}"   class="btn btn-info  col-md-offset-4" role="button"><span class="glyphicon glyphicon-arrow-left"></span>    Retour à la liste</a>

{% endblock %}
